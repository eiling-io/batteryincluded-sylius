{% extends '@SyliusAdmin/shared/crud/index.html.twig' %}

{% block content %}
    <div class="ui segment">
        <h1 class="ui header">BatteryIncluded Konfiguration</h1>
        {{ form_start(form) }}
            {{ form_row(form.collection) }}
            {{ form_row(form.api_key) }}
            <button class="ui primary button" type="submit">Speichern</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
